add OG to index.html
add normal fontSize manager (using mui theme - ?)
fix ugly article_modal (scrollable content, sizing on open and close)
add snackbars (show some errors to user)
add custom errors
fix autofetch on mobiles (article component)
add tests